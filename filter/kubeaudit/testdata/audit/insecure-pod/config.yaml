apiVersion: bluebrown.github.io/v1alpha1
kind: KubeAudit
metadata:
  name: security
  annotations:
    config.kubernetes.io/local-config: "true"
spec:
  enabledAuditors: {}
  auditors:
    limits:
      cpu: "750m"
      memory: "500m"
